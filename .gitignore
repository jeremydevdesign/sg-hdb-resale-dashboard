# Build inputs — generated by the scripts in the parent folder, inlined into
# the HTML at build time. The published site does not fetch these at runtime.
district_data.json
year_data.json

# macOS
.DS_Store
